name: PDFBox AOT Cache

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  aot-cache:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Download custom JDK
        run: |
          curl -L -o jdk-custom.tar.gz \
            "https://github.com/algomaster99/jdk/releases/download/jdk-supply-chain-aotcache-cf4948a6479c3a3274f79d62a33c320888a5e1e9/jdk-custom-cf4948a6479c3a3274f79d62a33c320888a5e1e9.tar.gz"

      - name: Set up custom JDK
        uses: actions/setup-java@v4
        with:
          distribution: jdkfile
          java-version: '25'
          jdkFile: jdk-custom.tar.gz

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build PDFBox
        working-directory: pdfbox
        run: mvn package -q

      - name: Check AOT cache
        working-directory: pdfbox
        run: |
          output=$(./check-aot.sh 2>&1)
          echo "$output"
          {
            echo "## AOT Cache Check"
            echo '```'
            echo "$output" | sed 's/\x1B\[[0-9;]*m//g'
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Performance check
        working-directory: pdfbox
        run: |
          output=$(./performance-check.sh 2>&1)
          echo "$output"
          {
            echo "## Performance Results"
            echo '```'
            echo "$output" | sed 's/\x1B\[[0-9;]*m//g'
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
